<h1> Artist Edit Form </h1>

    <%= form_for @artist do |f| %>
    <%= f.label 'Artist name: ' %>
    <%= f.text_field :name %>

    <%= f.label 'Artist Bio' %>
    <%= f.text_field :bio %>

    <%= f.submit "Update Artist" %>

    <% end %>