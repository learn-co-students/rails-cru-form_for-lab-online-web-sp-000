<h1>Edit Artist</h1><br>

<%= form_for(@artist) do |a| %>
    <%= a.label :name %>
    <%= a.text_field :name %>

    <%= a.label :bio %>
    <%= a.text_area :bio %>

    <%= a.submit "Update Artist" %>
<% end %>