<h1>Edit <%= @artist.name %></h1>

<%= form_for(@artist) do |f| %>
<%= f.label 'Name' %><br>
<%= f.text_field :name %>

<%= f.label 'Bio' %><br>
<%= f.text_area :bio %><br>

<%= f.submit %>
<% end %>
