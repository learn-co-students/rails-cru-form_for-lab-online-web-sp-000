<h3>Artist Form</h3>
<%= form_for(@artist) do |f| %>
<%= f.label :name, "Name:" %>
<%= f.text_field :name %><br>

<%= f.label :bio, "Bio:" %><br>
<%= f.text_area :bio %><br><br>

<%= f.submit %>
<% end %>