<h1>Edit Song</h1>

<%= form_for(@song) do |s| %>
    <%= s.label :name %>
    <%= s.text_field :name %>

    <%= s.label :artist %>
    <%= s.text_field :artist_id %>

    <%= s.label :genre %>
    <%= s.text_field :genre_id %>

    <%= s.submit "Update Song" %>
<% end %>