<h1>Song Form</h1>

<%= form_for :song, url: songs_path do |f| %>

    <%= f.text_field :name %>
    <%= f.number_field :artist_id, :id => "artist_id" %>
    <%= f.number_field :genre_id, :id => "genre_id" %>
    <%= f.submit "Create Song" %>


<% end %>