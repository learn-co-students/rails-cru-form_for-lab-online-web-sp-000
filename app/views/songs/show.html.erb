<h1><%= @song.name %></h1>
<p><%= link_to Artist.find(id=@song.artist_id).name, artist_path(@song.artist_id) %></p>
<p><%= link_to Genre.find(id=@song.genre_id).name, genre_path(@song.genre_id) %></p>
<%= params.inspect%>