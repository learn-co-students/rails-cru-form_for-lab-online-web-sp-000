<h1><%= @song.name %></h1>
<div><p><%= link_to @song.artist.name, artist_path(@song.artist_id) %></p></div>
<div><p><%= link_to @song.genre.name, genre_path(@song.genre_id) %></p></div>
<div><%= link_to "Edit Artist", edit_artist_path %></div>

have_link(@genre.name, href: genre_path(@genre))