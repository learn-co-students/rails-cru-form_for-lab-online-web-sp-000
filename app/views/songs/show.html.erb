<h1>Name: <%= @song.name %></h1>
<h2>My Artist: <%= (link_to @song.artist.name, artist_path(@song.artist)) if @song.artist %></h2>
<h2>My Genre: <%= (link_to @song.genre.name, genre_path(@song.genre)) if @song.genre %></h2>