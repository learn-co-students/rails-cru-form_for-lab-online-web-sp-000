<div>
    <h1>Song Name: <%= @song.name %></h1><br>
    <h3>Song Genre: <%= link_to @song.genre.name, genre_path(@song.genre) %></h3><br>
    <h3>Song Artist: <%= link_to @song.artist.name, artist_path(@song.artist) %></h3>
</div>
